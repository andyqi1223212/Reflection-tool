# Phase 4 验收清单对照（plan §1）

逐项对应 `phase4-inbox-ui.md` 第 27–39 行 checklist。

| # | 要求 | 状态 | 证据 / 操作 |
|---|------|------|----------------|
| 1 | `file://` 打开 `crystallization-prototype/inbox.html` 可见列表 | **已实现（fixture）** | 先 `python runs/_index.py`；用 Finder / Chrome 打开本地 HTML；列表应 3 条 sample |
| 2 | 列表来源 `<script src="../runs/_index.js">` | **已实现** | `inbox.html` 末两行 script；无 fetch |
| 3 | 每 run 显示 run_id / question_md 文件名 / status 徽章 / verdict 徽章 / 相对时间 | **已实现** | `inbox.js` `renderRunCard`：`question_basename`、`status-badge`、`verdict-badge`、`formatRelative` |
| 4 | 展开 details：6 维 scores 表 / fail_reasons / suggested_revisions / file:// JSON 链接 | **已实现** | `renderScoresTable` + `raw-links` + `encodeFileHref` |
| 5 | accept → modal 整行命令 + 剪贴板提示 | **已实现** | `python -m agents_runtime.orchestrate --resume <id> --from push --force-pass`；`#action-copy-status` |
| 6 | reject → modal `mv runs/<id> runs/_rejected/<id>` + refresh 提示 | **已实现** | `showActionModal("reject", …)` |
| 7 | chip 过滤 All / fail / conditional_pass / error | **已实现** | `VERDICT_CHIPS`；error = `is_error`（push_result 异常态） |
| 8 | 搜索框模糊匹配 question_md / run_id | **已实现** | `#inbox-search` + 150ms debounce |
| 9 | `index.html` 顶部 → inbox 链接 | **已实现** | `<nav class="top-nav">` + `inbox.css` link |
| 10 | 不做 mobile | **满足** | 未加响应式断点 |
| 11 | 颜色/字体/圆角复用 `styles.css` `:root` | **已实现** | `inbox.html` link `styles.css`；专有配色在 `inbox.css` 局部 hex |

## 本机 QA 步骤（建议）

```bash
cd /path/to/growth
./venv/bin/python3 runs/_index.py
open crystallization-prototype/inbox.html   # 或 Chrome 拖入
```

- 点 **conditional_pass** chip → 应剩 1 条 `sample_fixture_conditional_pass`
- 点 **accept** → 复制命令含 `sample_fixture_conditional_pass`
- 点 **error** → 应见 `sample_fixture_error_meta`（`mode_not_implemented`）

## 与 Phase 2 真跑衔接

| 条件 | 说明 |
|------|------|
| 有 `DEEPSEEK_API_KEY` 跑 orchestrator 产出 `awaiting_human` | 再执行 `runs/_index.py` refresh inbox |
| 无 API | 仅用 `runs/sample_fixture_*` 验收 UI |

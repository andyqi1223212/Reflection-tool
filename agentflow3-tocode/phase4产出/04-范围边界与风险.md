# 范围边界与风险（plan §3 + §6 + §7）

## §3 禁读 — 执行情况

| 禁读 | 遵守 |
|------|------|
| `agent第二轮/*.md` | 未读 |
| `agents_runtime/*.py`（实现） | 未读；仅 grep CLI 签名确认 `--force-pass` |
| `round2/*.py` | 未读 |
| `data/chains.json` | 未读 |
| Phase 3 eval / Phase 5 retrieval | 未依赖 |
| master / 其他 phase plan | 仅读 phase4 plan |

## §6 不在范围 — 确认未做

- 后端 / WebSocket / npm 构建 / 框架
- inbox 内执行 subprocess 或 py
- 改 `app.js` / `chains.data.js` / merge 逻辑
- README（plan 明确不写；本产出目录代替）
- mobile 响应式
- 自动在 orchestrate 末尾调 `_index.py`（留给后续）

## §7 风险与缓解

| 风险 | 缓解（本 phase） |
|------|------------------|
| 忘跑 `_index.py` | header `#last-refresh` 文案；空列表提示命令 |
| file:// fetch 禁用 | 方案 A，零 fetch |
| 重复 accept | modal 明示 refresh；merge exit 4 自保（Phase 2） |
| `<dialog>` 旧浏览器 | 不 polyfill；dogfood 用现代 Chrome |
| 中文路径 href | 分段 `encodeURIComponent` |
| manifest 缺 `a_summary` | 读 `a.json` 兜底 |
| `_index.js` 体积 | 当前 3 run ~3KB；1000 run 以后再分页 |
| reject 误操作 | `mv` 到 `_rejected/` 可逆；`_index.py` skip `_` 前缀目录 |

## 与 Phase 2 `04-范围边界` 的衔接

- Phase 2 不把 awaiting_human 自动 merge → Phase 4 做人审入口，**不**改变闸门逻辑。
- Phase 2 `runs/.gitignore` 忽略 run 产物 → Phase 4 fixture 用 `!sample_fixture_*` 例外，避免新人 clone 后 inbox 全空（仍须跑 `_index.py` 生成 `_index.js`）。

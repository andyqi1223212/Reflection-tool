# 摩擦与后续

## 已做

- [x] plan §4.1 四个交付文件 + 3 组 fixture
- [x] plan §4.2 `index.html` 最小侵入
- [x] `runs/.gitignore` 例外：`_index.py` / `_index.js` / `sample_fixture_*`
- [x] `_index.py` 超出 plan 草图：`is_error`、`a.json` 兜底、`question_basename`
- [x] `phase4产出/` 全套 crosscheck 文档

## 摩擦

1. **`runs/.gitignore` 原为 `*`**：clone 后无 `_index.js`，必须文档化「先跑 `_index.py`」——已在 inbox header 与 `01` QA 写明。
2. **plan §5.5 两种 index 改法**：采用「index 加 `inbox.css` link」而非改 `styles.css`，与 plan workhorse 选项一致。
3. **error chip vs verdict=pass**：`sample_fixture_error_meta` 在 judge 为 pass 但 push `mode_not_implemented`；用 `is_error` 而非 verdict 进 error 桶，避免与 All 列表语义冲突。
4. **剪贴板在 file://**：部分浏览器限制 clipboard API；已实现 `execCommand` 回退。

## 未做（留给后续 phase / friction）

- [ ] orchestrate 在写入 `awaiting_human` 后自动调用 `runs/_index.py`（plan §7 备忘）
- [ ] `manifest.a_summary` 由 Phase 2 正式写入（当前 `a.json` 兜底够用）
- [ ] 用真实 API 跑一条 conditional_pass → 对照 inbox 与 `--list-pending` 是否一致
- [ ] pagination（run >100）
- [ ] `reject` 一键脚本（当前仅 mv 命令，符合 master plan「人在最后一关」）

## 建议用户下一步

1. `open crystallization-prototype/inbox.html` 走 fixture QA。
2. 有 API 时跑一条 non-pass orchestrator → `runs/_index.py` → refresh inbox。
3. accept 后在终端执行复制的命令 → refresh → 确认 run 从列表消失。

# Phase 2 验收清单对照（plan §1）

以下逐项对应 `phase2-orchestrator.md` 第 30–41 行 checklist。

| # | 要求 | 状态 | 证据 / 操作 |
|---|------|------|----------------|
| 1 | `python -m agents_runtime.orchestrate 外部source/球场垃圾话应对策略.md` 跑通全链；末尾打印 run_id + stages + verdict + UI URL（若 pass） | **需本机 `DEEPSEEK_API_KEY`** | 实现：`agents_runtime/orchestrate.py` → `run_single_case`；无 key 时在 `run_a` 失败，manifest `last_error` 可追。有 key 时在仓库根执行；成功 pass 且非 `--no-push` 时 `res["ui_line"]` 来自 merge stdout。CLI 摘要见 `_print_summary`。 |
| 2 | 跑完后 `runs/2026-MM-DD_ball-trash-talk_<hash>/` 含 `input.json` / `route_helper.json` / `a.json` / `b.json` / `judge.json` / `manifest.json`（pass 且已 push 时多 `push.json`） | **逻辑已实现**；真跑依赖 #1 | `run_state.save_input_json`；各 stage `_write_json`；`push.json` 仅 merge 成功写入。单测 `test_orchestrate_dry` 验证目录与 manifest。 |
| 3 | `route=update` case：A 输出 `route=update` 后 orchestrator **自动**从 `data/chains.json` 按 `target_ic_id` 取卡填 B/Judge 的 `existing_card`，人类 0 次手动贴 | **已实现** | `orchestrate.run_stage_b` / `run_stage_judge` 调用 `_load_existing_card`；找不到则 `FileNotFoundError` → manifest `failed` + `next_action`。 |
| 4 | 中途 `Ctrl+C` 后 `--resume <run_id>` 从下一未完成 stage 继续；`completed_stages` 仅在 stage **成功结束**后 append | **已实现 resume / completed 语义** | `run_single_case(..., resume=True, from_stage=...)`；`KeyboardInterrupt` 时写 `status=failed` 后重新抛出（见 #4 摩擦）。 |
| 5 | 任一 stage 失败：`manifest` 含 `last_stage` / `last_error` / `next_action`；非 0 exit；不继续 | **已实现** | 内层 `except` 写 `last_stage` + `current_stage` + `failed` 后 `break`；`_cli` 对 `status==failed` → `SystemExit(1)`。 |
| 6 | `verdict==pass`：自动 `run_pipeline.py merge`；`--mode update` 当且仅当 `b.output_kind=="update_entry"` | **已实现** | `run_stage_push` → `_subprocess.run_merge(..., mode_update=...)`。 |
| 7 | `verdict ∈ {conditional_pass, fail}`：不调 merge；`status=awaiting_human`；打印提示 | **已实现**（摘要经 `_print_summary`） | `test_conditional_pass_awaiting_human`；manifest `next_action` 含 inbox 提示语。 |
| 8 | `--no-push`：即便 pass 也不 merge | **已实现** | `test_pass_no_push_skips_merge`。 |
| 9 | `--from <stage>` 必须配合 `--resume`；强制从指定 stage 重跑并清后续产物 | **已实现** | `_truncate_completed_for_resume` + `_delete_artifacts_from`；`test_resume_from_truncates_and_deletes`；CLI `ap.error` 校验。 |
| 10 | 3 个历史 question_md 跑通并与 `agents/runs/run_2026-05-*` verdict 同向 | **需 API + 人工对照** | 与 Phase 1 相同约束；参见 `phase1产出/03-黄金对照与Schema漂移.md`（黄金为 v1 形态，仅 verdict 同向为合理目标）。 |

**pytest（不耗 API）**

```bash
./venv/bin/python3 -m pytest agents_runtime/tests/ -q
```

当前：**13 passed**（含 `test_run_state`、`test_orchestrate_dry`）。

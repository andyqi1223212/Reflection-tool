# 范围、边界与风险（plan §2 + §3 + §6 + §7 + §9）

## plan §2 必读输入（workhorse 上下文）

本产出编写时对照了：本 plan、`agents.py` 三函数签名、`run_pipeline.py` 的 `cmd_merge`、`route_helper` CLI、`data/chains.json` 顶层结构、`agents/runs` 三份黄金 JSON。  
**未读** plan §3 禁读列表中的 prompt 全文（与 plan 纪律一致）。

## plan §3 禁读列表

- 实现代码路径上 **未 import** `loader` / `llm_client` / `context_builder`（仅 `agents` 与 `_subprocess`）。

## plan §6 不在范围（确认未做）

| 条款 | 状态 |
|------|------|
| 不改 `round2/{run_pipeline,route_helper,next_ic_id}.py` | 满足 |
| 不改 `agent第二轮/push.prompt.md` | 满足 |
| 无 inbox UI | 满足 |
| 无 retrieval / eval 批跑 | 满足 |
| 不重包 LLM、不重判 verdict | 满足 |
| 无 asyncio / pydantic | 满足 |
| 不写总 README | 满足（产出在本目录） |

## plan §7 风险 ↔ 缓解

| 风险 | 缓解在代码中的体现 |
|------|---------------------|
| route_helper JSON 升级 | stdout 整段 `json.loads` 存盘；A 侧消费由 Phase 1 `run_a` 容错 |
| `chains.json` 顶层变化 | `_load_existing_card` 校验 `chains` 为 list |
| venv 路径 | `sys.executable` 调子进程（`_subprocess.py`） |
| run_id 碰撞 | `time.time()` + 重试循环 |
| Ctrl+C 与 manifest | `KeyboardInterrupt` 写 `failed` + `last_stage` + `next_action` |
| `raw_answer_seeds` 子结构 | `_route_context_from_a` 整键赋值 |
| `meta_card` | push 分支 `mode_not_implemented`，`awaiting_human` |
| 中文路径 | `pathlib` + slug 正则保留 `\u4e00-\u9fa5` |

## plan §9 实施顺序（实际）

1. `run_state.py` + `test_run_state.py`
2. `_subprocess.py`（merge / route_helper）
3. `orchestrate.py` 骨架 + CLI
4. 五 stage 实现 + `run_single_case`
5. resume / `--from`
6. `test_orchestrate_dry.py`
7. 真机 3 case：待 API（与 plan 预估一致）

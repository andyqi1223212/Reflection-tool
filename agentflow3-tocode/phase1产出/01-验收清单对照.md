# Phase 1 验收清单对照（plan §1）

以下逐项对应 `phase1-prompt-callable.md` 第 29–39 行。  
**本机 E2E 记录**：[06-本机E2E-run_a记录.md](./06-本机E2E-run_a记录.md)

| # | 要求 | 状态 | 证据 / 操作 |
|---|------|------|----------------|
| 1 | `run_a` + `question_md` + **有效** `route_helper_output` 跑出 A draft JSON | **本机已通过（2026-05-19）** | 两参 CLI：`run_a 外部source/球场垃圾话应对策略.md /tmp/route_helper.json`。快照：[run_2026-05-19_e2e_pipeline-a_ball-trash-talk.json](./run_2026-05-19_e2e_pipeline-a_ball-trash-talk.json)。仅单参且 `{}` 时会得 `missing_route_helper`（契约预期）。 |
| 2 | 与黄金 A 比 `axis` / `route` / `patterns`；`mechanism_sketch` 语义同向 | **本机已对照（部分自动）** | `axis=attention` 一致；`patterns` 集合与黄金不完全相同（见 06）；`route=update`+`IC-024` 因库内已有卡 + helper high，与黄金 v1「隐式 new」不同但合理。脚本：`compare_a_ball_trash.py`（比 axis/patterns；黄金无 route）。 |
| 3 | `run_b <a_output.json>` 对照 B 黄金 | **待本机 API** | 本 run 为 **update** → B 需 `existing_card`（`data/chains.json` 中 IC-024）。黄金 B：`agents/runs/run_2026-05-12_pipeline-b_ball-trash-talk.json`。 |
| 4 | `run_judge` 对照 judge 黄金 | **待本机 API** | update 路径用 [route_context.example.update_ic024.json](./route_context.example.update_ic024.json)。 |
| 5 | `forbidden_inputs` lint | **已实现** | 单测 `test_context_builder.py`。 |
| 6 | DeepSeek 模型档位 + `response_format=json_object` | **已实现** | `agents.py` + `llm_client.py`（默认 v4-pro / v4-flash）。 |
| 7 | JSON parse retry + debug 落盘 | **已实现** | `test_llm_client_mock.py`。 |
| 8 | `parse_stats.jsonl` | **已实现** | 真跑后见 `agents_runtime/_stats/parse_stats.jsonl`。 |
| 9 | pytest 全 pass | **已实现** | `./venv/bin/python3 -m pytest agents_runtime/tests/ -q` → 8 passed。 |

**依赖**：`pyyaml`；`.env` 中 `DEEPSEEK_API_KEY`；A 必先 `route_helper`（见 [02-环境与CLI.md](./02-环境与CLI.md)）。

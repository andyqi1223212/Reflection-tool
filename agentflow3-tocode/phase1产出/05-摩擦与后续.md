# 摩擦与后续

## 本机已执行（2026-05-19）

- **DeepSeek `run_a` E2E**：`route_helper` → `run_a` 两参 CLI，球场垃圾话场景；产出见 [06-本机E2E-run_a记录.md](./06-本机E2E-run_a记录.md) 与 [run_2026-05-19_e2e_pipeline-a_ball-trash-talk.json](./run_2026-05-19_e2e_pipeline-a_ball-trash-talk.json)。
- **摩擦（已接受）**：终端长时间无输出 = 等模型；单参 `run_a` 得 `missing_route_helper` = 契约预期。
- **路由差异**：相对黄金 v1 为 `route=update`+`IC-024`（库内已有卡 + helper confidence=high），用户主观认为诊断质量可接受。

## 仍未执行（Phase 1 checklist 余项）

- **`run_b` / `run_judge` 本机 API**：A 为 update 时 B 必须喂 `data/chains.json` 中 IC-024 的 **existing_card_json**。
- **`parse_stats.jsonl` fail 率汇总**：多次真跑后统计（plan §1 末句）。

## 已知实现注意点

1. **`v3_fewshot` 仍可为空列表**：上下文有占位句；生产由 Phase 5 / Phase 2 传入 fewshot。
2. **`run_b` 默认 `route=new`**：仅当 A JSON 缺 `route` 时触发；本机 E2E 已带 `update`，不会误用默认。
3. **Windows 路径**：未测。

## Phase 2 衔接

- `orchestrate` 应串联：`route_helper` subprocess → `run_a(..., route_helper_output=...)` → 按 `route` 从 `chains.json` 取卡 → `run_b` → `run_judge`。
- 本机 A 快照可直接作为 Phase 2 dry-run 的 `a_output` 夹具（需剥 `_meta` 若过 schema 校验）。
